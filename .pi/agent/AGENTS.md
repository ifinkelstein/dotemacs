# Emacs Config Agent Instructions

## Always Commit
After making any changes to files in this repo, always stage and commit:

```bash
cd /Users/ilya/.config/.emacs && git add -A && git commit -m "descriptive message"
```
